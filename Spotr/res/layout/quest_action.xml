<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#181818"
    android:orientation="vertical"
    android:padding="10dp" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="200sp"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/quest_action_xml_image"
            android:layout_width="90sp"
            android:layout_height="90sp"
            android:padding="2dp"
            android:scaleType="fitXY"
            android:src="@drawable/ic_main_menu_map_default" />

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/quest_action_xml_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="4dp"
                android:textColor="#cccccc"
                android:textSize="20dp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/quest_action_xml_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="4dp"
                android:textColor="#95a5c6"
                android:textSize="12dp"
                android:textStyle="bold" />
        </LinearLayout>

        
    </LinearLayout>
    <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:background="#333333"
            android:paddingLeft="4dp"
            android:text="Missions"
            android:textSize="15dp"
            android:textColor="#cccccc"
            android:textStyle="bold" />
    <!-- Challenge List -->

    <ListView
        android:id="@+id/quest_action_xml_listview_actions"
        style="@style/RoundedRectListView.MissionsList" >
    </ListView>

</LinearLayout>